function result = shift(matrix,y,x)% % SHIFT  scrolls and pans a matrix% % usage:%     RESULT = SHIFT(M, Y, X) shifts M by Y elements vertically and%                               X elements horizontally%     % see also: roll, turn, shear% % Scott B. Stevenson% history:% 9/24/00   sbs Wrote it.% 7/3/02    lkc Cleaned up the comments.[m n] = size(matrix);y = rem(y+m,m);y = rem(y+m,m);x = rem(x+n,n);x = rem(x+n,n); %second time to trap negative 1result = [matrix(y+1:m,:); matrix(1:y,:)];result = [result(:,x+1:n) result(:,1:x)]; 